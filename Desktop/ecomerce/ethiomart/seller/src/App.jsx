import React from 'react'

export default function App(){
  return (
    <div style={{padding:20}}>
      <h1>Ethiomart — Seller Dashboard</h1>
      <p>Run `npm install` then `npm run dev`.</p>
    </div>
  )
}
